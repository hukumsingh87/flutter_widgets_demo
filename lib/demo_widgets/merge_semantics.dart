//!MergeSemantics

import 'package:flutter/material.dart';

class Widget136 extends StatelessWidget {
  const Widget136({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const MergeSemantics(
        child: Row(
          children: [
            Icon(Icons.person),
            Text('Flutter Mapp', style: TextStyle(fontSize: 18),),
            // Text('Flutter Mapp', style: TextStyle(fontSize: 18),),
            // Text('Flutter Mapp', style: TextStyle(fontSize: 18),),
            // Text('Flutter Mapp', style: TextStyle(fontSize: 18),),
          ],
        ),
      ),
    );
  }
}
